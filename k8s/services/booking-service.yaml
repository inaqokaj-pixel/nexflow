apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: nexflow
  labels:
    app: booking-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
    spec:
      containers:
        - name: booking-service
          image: nexflow/booking-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3003
          env:
            - name: PORT
              value: "3003"
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: nexflow-config
                  key: NODE_ENV
            - name: DB_HOST
              value: "postgres-booking"
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: "booking_db"
            - name: DB_USER
              value: "postgres"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nexflow-secrets
                  key: postgres-password
            - name: RABBITMQ_URL
              valueFrom:
                configMapKeyRef:
                  name: nexflow-config
                  key: RABBITMQ_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 8
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 15
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: nexflow
spec:
  selector:
    app: booking-service
  ports:
    - port: 3003
      targetPort: 3003
